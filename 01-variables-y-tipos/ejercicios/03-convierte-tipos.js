/**
 * EJERCICIO 03 — Convierte tipos
 *
 * Escribí tres funciones de conversión explícita:
 *
 * 1. `aNumero(texto)`  → convierte un string a número con Number()
 * 2. `aTexto(valor)`   → convierte cualquier valor a string con String()
 * 3. `aBooleano(valor)`→ convierte cualquier valor a booleano con Boolean()
 *
 * ➡️ Ejemplos:
 *    aNumero('42')     →  42
 *    aTexto(42)        →  '42'
 *    aBooleano(0)      →  false
 *    aBooleano(1)      →  true
 */

// TODO: completa las tres funciones

function aNumero(texto) {
  // 👇 escribí tu código aquí
}

function aTexto(valor) {
  // 👇 escribí tu código aquí
}

function aBooleano(valor) {
  // 👇 escribí tu código aquí
}

// No modifiques esta línea (el test la necesita):
module.exports = { aNumero, aTexto, aBooleano };