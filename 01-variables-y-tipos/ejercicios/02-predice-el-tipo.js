/**
 * EJERCICIO 02 — Predice el tipo
 *
 * Escribí la función `tipoDeValor` que reciba cualquier valor y devuelva
 * el resultado de `typeof` sobre ese valor.
 *
 * ➡️ Ejemplos:
 *    tipoDeValor(42)          →  'number'
 *    tipoDeValor('hola')      →  'string'
 *    tipoDeValor(true)        →  'boolean'
 *    tipoDeValor(undefined)   →  'undefined'
 */

// TODO: completa la función

function tipoDeValor(valor) {
  // 👇 escribí tu código aquí (una sola línea con "return ...")

}

// No modifiques esta línea (el test la necesita):
module.exports = { tipoDeValor };