/**
 * SOLUCIÓN 02 — Predice el tipo
 *
 * 👀 Mirada solo DESPUÉS de intentarlo. Reescribila de memoria en tu editor.
 */

function tipoDeValor(valor) {
  return typeof valor;
}

module.exports = { tipoDeValor };