/**
 * SOLUCIÓN 04 — Plantilla de saludo
 *
 * 👀 Mirada solo DESPUÉS de intentarlo. Reescribila de memoria en tu editor.
 */

function saludo(nombre) {
  return `¡Hola, ${nombre}! Estás aprendiendo JavaScript.`;
}

module.exports = { saludo };