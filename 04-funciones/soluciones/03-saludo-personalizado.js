/**
 * SOLUCIÓN 03 — Saludo personalizado
 *
 * 👀 Mirada solo DESPUÉS de intentarlo. Reescribila de memoria en tu editor.
 */

function saludoPersonalizado(nombre, prefijo = 'Hola') {
  return `${prefijo}, ${nombre}!`;
}

module.exports = { saludoPersonalizado };