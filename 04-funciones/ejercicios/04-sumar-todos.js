/**
 * EJERCICIO 04 — Sumar todos los números
 *
 * Completá la función `sumarTodos` para que devuelva la suma de TODOS los
 * argumentos recibidos. Usá el parámetro `rest` (`...numeros`) para juntarlos
 * en un array. Si no recibe ningún argumento, debe devolver 0.
 *
 * Podés usar un bucle (`for`, `for...of`) o el método `reduce`.
 *
 * ➡️ Ejemplos:
 *    sumarTodos(1, 2, 3)   →  6
 *    sumarTodos(10, 20)    →  30
 *    sumarTodos()          →  0
 */

// TODO: completá la función

function sumarTodos(...numeros) {
  // 👇 escribí tu código aquí

}

// No modifiques esta línea (el test la necesita):
module.exports = { sumarTodos };