/**
 * EJERCICIO 05 — Aplicar una operación
 *
 * Completá la función `aplicarOperacion` para que reciba dos valores y una
 * función `operacion`, la invoque con `(a, b)` y devuelva su resultado.
 *
 * Luego usala con arrow functions inline.
 *
 * ➡️ Ejemplos:
 *    aplicarOperacion(4, 3, (a, b) => a + b)   →  7
 *    aplicarOperacion(4, 3, (a, b) => a * b)   →  12
 *    aplicarOperacion(10, 2, (a, b) => a / b)  →  5
 */

// TODO: completá la función

function aplicarOperacion(a, b, operacion) {
  // 👇 escribí tu código aquí

}

// No modifiques esta línea (el test la necesita):
module.exports = { aplicarOperacion };