/**
 * EJERCICIO 05 — Serializar y deserializar
 *
 * Escribí DOS funciones:
 *
 * 1. `serializar(objeto)`  →  devuelve `JSON.stringify(objeto)`
 *    (convierte el objeto a un TEXTO en formato JSON)
 *
 * 2. `deserializar(texto)`  →  devuelve `JSON.parse(texto)`
 *    (convierte un TEXTO JSON válido a objeto)
 *
 * ➡️ Ejemplos:
 *    serializar({ nombre: 'Ana', edad: 30 })
 *      →  '{"nombre":"Ana","edad":30}'
 *
 *    deserializar('{"nombre":"Ana","edad":30}')
 *      →  { nombre: 'Ana', edad: 30 }
 */

// TODO: completa ambas funciones

function serializar(objeto) {
  // 👇 escribí tu código aquí

}

function deserializar(texto) {
  // 👇 escribí tu código aquí

}

// No modifiques esta línea (el test la necesita):
module.exports = { serializar, deserializar };