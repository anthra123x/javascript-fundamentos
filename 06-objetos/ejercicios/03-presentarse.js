/**
 * EJERCICIO 03 — Presentarse
 *
 * Escribí la función `presentarse` que reciba un objeto `persona`
 * que tiene las propiedades `nombre` y `edad`, y devuelva el texto:
 *
 *    "Hola, soy {nombre} y tengo {edad} años."
 *
 * Usá un TEMPLATE LITERAL (backticks con ${...}).
 *
 * ➡️ Ejemplos:
 *    presentarse({ nombre: 'Ana', edad: 30 })
 *      →  'Hola, soy Ana y tengo 30 años.'
 *
 *    presentarse({ nombre: 'Leo', edad: 25 })
 *      →  'Hola, soy Leo y tengo 25 años.'
 */

// TODO: completa la función

function presentarse(persona) {
  // 👇 escribí tu código aquí

}

// No modifiques esta línea (el test la necesita):
module.exports = { presentarse };