/**
 * EJERCICIO 01 — Crear un usuario
 *
 * Escribí la función `crearUsuario` que reciba un `nombre` y una `edad`,
 * y devuelva un OBJETO con esas dos propiedades.
 *
 * Usá el SHORTHAND (atajo) de objetos: cuando la propiedad y la variable
 * se llaman igual, podés escribir solo `nombre` en vez de `nombre: nombre`.
 *
 * ➡️ Ejemplos:
 *    crearUsuario('Ana', 30)  →  { nombre: 'Ana', edad: 30 }
 *    crearUsuario('Leo', 25)  →  { nombre: 'Leo', edad: 25 }
 */

// TODO: completa la función

function crearUsuario(nombre, edad) {
  // 👇 escribí tu código aquí

}

// No modifiques esta línea (el test la necesita):
module.exports = { crearUsuario };