/**
 * SOLUCIÓN 03 — Duplicar números
 *
 * 👀 Mirada solo DESPUÉS de intentarlo. Reescribila de memoria en tu editor.
 */

function duplicarNumeros(numeros) {
  return numeros.map((numero) => numero * 2);
}

module.exports = { duplicarNumeros };