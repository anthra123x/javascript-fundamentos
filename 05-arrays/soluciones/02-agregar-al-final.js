/**
 * SOLUCIÓN 02 — Agregar al final
 *
 * 👀 Mirada solo DESPUÉS de intentarlo. Reescribila de memoria en tu editor.
 */

function agregarAlFinal(arr, item) {
  return arr.push(item);
}

module.exports = { agregarAlFinal };