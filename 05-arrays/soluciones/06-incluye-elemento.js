/**
 * SOLUCIÓN 06 — Incluye elemento
 *
 * 👀 Mirada solo DESPUÉS de intentarlo. Reescribila de memoria en tu editor.
 */

function incluyeElemento(arr, item) {
  return arr.includes(item);
}

module.exports = { incluyeElemento };