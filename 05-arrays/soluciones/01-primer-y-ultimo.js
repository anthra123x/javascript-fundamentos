/**
 * SOLUCIÓN 01 — Primer y último elemento
 *
 * 👀 Mirada solo DESPUÉS de intentarlo. Reescribila de memoria en tu editor.
 */

function primerElemento(arr) {
  return arr[0];
}

function ultimoElemento(arr) {
  return arr[arr.length - 1];
}

module.exports = { primerElemento, ultimoElemento };