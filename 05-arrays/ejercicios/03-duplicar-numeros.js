/**
 * EJERCICIO 03 — Duplicar números
 *
 * Completá `duplicarNumeros(numeros)` para que use `map` y devuelva un array
 * nuevo con cada número multiplicado por 2. NO modifiques el array original.
 *
 * ➡️ Ejemplos:
 *    duplicarNumeros([1, 2, 3])     →  [2, 4, 6]
 *    duplicarNumeros([])            →  []
 *    duplicarNumeros([-1, 0, 5])    →  [-2, 0, 10]
 */

// TODO: completá la función

function duplicarNumeros(numeros) {
  // 👇 escribí tu código aquí

}

// No modifiques esta línea (el test la necesita):
module.exports = { duplicarNumeros };