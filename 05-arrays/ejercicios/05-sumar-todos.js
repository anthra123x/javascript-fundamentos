/**
 * EJERCICIO 05 — Sumar todos los números
 *
 * Completá `sumarTodos(numeros)` para que use `reduce` y devuelva la suma de
 * todos los números del array. Si el array está vacío, debe devolver 0.
 *
 * ➡️ Ejemplos:
 *    sumarTodos([1, 2, 3])   →  6
 *    sumarTodos([10, 20])    →  30
 *    sumarTodos([])          →  0
 */

// TODO: completá la función

function sumarTodos(numeros) {
  // 👇 escribí tu código aquí

}

// No modifiques esta línea (el test la necesita):
module.exports = { sumarTodos };