/**
 * EJERCICIO 06 — Incluye elemento
 *
 * Completá `incluyeElemento(arr, item)` para que devuelva `true` si `item`
 * está dentro de `arr` y `false` si no. Usá el método `includes`.
 *
 * ➡️ Ejemplos:
 *    incluyeElemento(['a', 'b'], 'a')  →  true
 *    incluyeElemento(['a', 'b'], 'z')  →  false
 *    incluyeElemento([], 'x')          →  false
 */

// TODO: completá la función

function incluyeElemento(arr, item) {
  // 👇 escribí tu código aquí

}

// No modifiques esta línea (el test la necesita):
module.exports = { incluyeElemento };