/**
 * EJERCICIO 01 — Primer y último elemento
 *
 * Completá `primerElemento(arr)` para que devuelva el primer elemento del
 * array, y `ultimoElemento(arr)` para que devuelva el último (usá `length`).
 *
 * ➡️ Ejemplos:
 *    primerElemento(['a', 'b', 'c'])  →  'a'
 *    ultimoElemento(['a', 'b', 'c'])  →  'c'
 *    ultimoElemento([])               →  undefined
 */

// TODO: completá las funciones

function primerElemento(arr) {
  // 👇 escribí tu código aquí

}

function ultimoElemento(arr) {
  // 👇 escribí tu código aquí

}

// No modifiques esta línea (el test la necesita):
module.exports = { primerElemento, ultimoElemento };