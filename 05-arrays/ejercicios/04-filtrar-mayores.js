/**
 * EJERCICIO 04 — Filtrar mayores
 *
 * Completá `filtrarMayores(numeros, minimo)` para que use `filter` y
 * devuelva un array nuevo SOLO con los números estrictamente mayores que
 * `minimo`. NO modifiques el array original.
 *
 * ➡️ Ejemplos:
 *    filtrarMayores([1, 5, 8, 2], 3)   →  [5, 8]
 *    filtrarMayores([10, 4, 7], 4)     →  [10, 7]
 *    filtrarMayores([], 0)             →  []
 */

// TODO: completá la función

function filtrarMayores(numeros, minimo) {
  // 👇 escribí tu código aquí

}

// No modifiques esta línea (el test la necesita):
module.exports = { filtrarMayores };