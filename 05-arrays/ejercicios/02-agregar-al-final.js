/**
 * EJERCICIO 02 — Agregar al final
 *
 * Completá `agregarAlFinal(arr, item)` para que use `push` para agregar
 * `item` al final de `arr` y devuelva el NUEVO largo del array (lo que
 * devuelve `push`).
 *
 * ➡️ Ejemplos:
 *    agregarAlFinal([1, 2], 3)  →  3
 *    agregarAlFinal([], 'hola') →  1
 */

// TODO: completá la función

function agregarAlFinal(arr, item) {
  // 👇 escribí tu código aquí

}

// No modifiques esta línea (el test la necesita):
module.exports = { agregarAlFinal };