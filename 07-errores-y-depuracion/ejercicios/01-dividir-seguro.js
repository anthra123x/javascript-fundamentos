/**
 * EJERCICIO 01 — División segura
 *
 * Escribí la función `dividirSeguro` que reciba dos números `a` y `b`.
 *
 * Regla: si `b` es exactamente 0, NO dividas: devolvé el string
 * 'Error: división entre cero'. En cualquier otro caso, devolvé `a / b`.
 *
 * La idea es defender la función por adelantado (defensive programming):
 * evitamos el error matemático sin lanzar excepciones.
 *
 * ➡️ Ejemplos:
 *    dividirSeguro(10, 2)   →  5
 *    dividirSeguro(10, 0)   →  'Error: división entre cero'
 */

// TODO: completa la función

function dividirSeguro(a, b) {
  // 👇 escribí tu código aquí

}

// No modifiques esta línea (el test la necesita):
module.exports = { dividirSeguro };