/**
 * EJERCICIO 02 — Día de la semana
 *
 * Escribí la función `diaSemana(numero)` que devuelva el nombre del día usando
 * switch:
 *
 *   1 → 'Lunes'   2 → 'Martes'   3 → 'Miércoles'   4 → 'Jueves'
 *   5 → 'Viernes' 6 → 'Sábado'   7 → 'Domingo'
 *   cualquier otro → 'Día inválido'
 *
 * ➡️ Ejemplos:
 *    diaSemana(1)  →  'Lunes'
 *    diaSemana(5)  →  'Viernes'
 *    diaSemana(9)  →  'Día inválido'
 *    diaSemana(0)  →  'Día inválido'
 */

// TODO: completá la función

function diaSemana(numero) {
  // 👇 escribí tu código aquí

}

// No modifiques esta línea (el test la necesita):
module.exports = { diaSemana };