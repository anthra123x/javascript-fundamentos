/**
 * EJERCICIO 05 — Primeros múltiplos
 *
 * Escribí la función `primerosMultiplos(numero, cantidad)` que devuelva un ARRAY
 * con los primeros `cantidad` múltiplos de `numero` (empezando desde `numero * 1`).
 *
 * Usá un bucle (for o while) y el método `push` para armar el array.
 *
 * ➡️ Ejemplos:
 *    primerosMultiplos(3, 4)  →  [3, 6, 9, 12]
 *    primerosMultiplos(5, 3)  →  [5, 10, 15]
 *    primerosMultiplos(7, 1)  →  [7]
 */

// TODO: completá la función

function primerosMultiplos(numero, cantidad) {
  // 👇 escribí tu código aquí

}

// No modifiques esta línea (el test la necesita):
module.exports = { primerosMultiplos };