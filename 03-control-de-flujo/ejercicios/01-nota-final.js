/**
 * EJERCICIO 01 — Nota final
 *
 * Escribí la función `notaFinal(calificacion)` que convierta un número en una
 * letra usando if/else if/else:
 *
 *   calificacion >= 90  →  'A'
 *   calificacion >= 80  →  'B'
 *   calificacion >= 70  →  'C'
 *   calificacion >= 60  →  'D'
 *   cualquier otro caso →  'F'
 *
 * ➡️ Ejemplos:
 *    notaFinal(95)  →  'A'
 *    notaFinal(82)  →  'B'
 *    notaFinal(60)  →  'D'
 *    notaFinal(45)  →  'F'
 */

// TODO: completá la función

function notaFinal(calificacion) {
  // 👇 escribí tu código aquí

}

// No modifiques esta línea (el test la necesita):
module.exports = { notaFinal };