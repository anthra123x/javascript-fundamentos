/**
 * EJERCICIO 04 — Cuenta regresiva
 *
 * Escribí la función `cuentaRegresiva(inicio)` que devuelva un STRING con los
 * números desde `inicio` hasta 0, separados por comas y SIN coma al final.
 *
 * Usá un bucle `while`. Pista: construí el string concatenando cada número y
 * agregá la coma solo cuando NO sea el último.
 *
 * ➡️ Ejemplos:
 *    cuentaRegresiva(3)  →  '3,2,1,0'
 *    cuentaRegresiva(5)  →  '5,4,3,2,1,0'
 *    cuentaRegresiva(0)  →  '0'
 */

// TODO: completá la función

function cuentaRegresiva(inicio) {
  // 👇 escribí tu código aquí

}

// No modifiques esta línea (el test la necesita):
module.exports = { cuentaRegresiva };