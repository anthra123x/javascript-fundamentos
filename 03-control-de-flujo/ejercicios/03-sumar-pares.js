/**
 * EJERCICIO 03 — Sumar pares
 *
 * Escribí la función `sumarPares(limite)` que sume TODOS los números pares
 * desde 0 hasta `limite` (inclusive) usando un bucle `for`.
 *
 * La suma debe empezar en 0 y acumularse con `+=`.
 *
 * ➡️ Ejemplos:
 *    sumarPares(10)  →  30   (2 + 4 + 6 + 8 + 10)
 *    sumarPares(7)   →  12   (2 + 4 + 6)
 *    sumarPares(5)   →  6    (2 + 4)
 *    sumarPares(0)   →  0
 */

// TODO: completá la función

function sumarPares(limite) {
  // 👇 escribí tu código aquí

}

// No modifiques esta línea (el test la necesita):
module.exports = { sumarPares };