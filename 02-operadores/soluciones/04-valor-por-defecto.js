/**
 * SOLUCIÓN 04 — Valor por defecto
 *
 * 👀 Mirala solo DESPUÉS de intentarlo. Reescribila de memoria en tu editor.
 */

function valorPorDefecto(valor) {
  return valor || 'por defecto';
}

module.exports = { valorPorDefecto };