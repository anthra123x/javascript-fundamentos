/**
 * SOLUCIÓN 03 — ¿Puede conducir?
 *
 * 👀 Mirala solo DESPUÉS de intentarlo. Reescribila de memoria en tu editor.
 */

function puedeConducir(edad, tieneLicencia) {
  return edad >= 18 && tieneLicencia;
}

module.exports = { puedeConducir };