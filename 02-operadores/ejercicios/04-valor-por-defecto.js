/**
 * EJERCICIO 04 — Valor por defecto
 *
 * Escribí la función `valorPorDefecto(valor)` que devuelva el `valor` si es
 * "truthy" y el string `'por defecto'` si es "falsy" (0, '', null, undefined,
 * false, NaN).
 *
 * Pista: el patrón clásico es `valor || 'por defecto'`.
 *
 * ➡️ Ejemplos:
 *    valorPorDefecto('texto')   →  'texto'
 *    valorPorDefecto(42)        →  42
 *    valorPorDefecto(null)      →  'por defecto'
 *    valorPorDefecto('')        →  'por defecto'
 *    valorPorDefecto(0)         →  'por defecto'
 */

// TODO: completá la función

function valorPorDefecto(valor) {
  // 👇 escribí tu código aquí

}

// No modifiques esta línea (el test la necesita):
module.exports = { valorPorDefecto };