/**
 * EJERCICIO 03 — ¿Puede conducir?
 *
 * Escribí la función `puedeConducir(edad, tieneLicencia)` que devuelva `true`
 * solo si la persona es mayor de edad (edad >= 18) Y tiene licencia.
 *
 * Pensalo así: para conducir se necesitan LAS DOS condiciones a la vez →
 * usá el operador && (y en español).
 *
 * ➡️ Ejemplos:
 *    puedeConducir(18, true)   →  true
 *    puedeConducir(18, false)  →  false
 *    puedeConducir(17, true)   →  false
 *    puedeConducir(20, true)   →  true
 */

// TODO: completá la función

function puedeConducir(edad, tieneLicencia) {
  // 👇 escribí tu código aquí

}

// No modifiques esta línea (el test la necesita):
module.exports = { puedeConducir };